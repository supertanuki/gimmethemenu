{% extends '::base.html.twig' %}

{% block title %}{{ page.seoTitle|default(page.title) }}{% endblock %}

{% block body %}
    <h1>{{ page.title }}</h1>
    <div>
        {{ page.body|raw }}
    </div>
{% endblock %}
